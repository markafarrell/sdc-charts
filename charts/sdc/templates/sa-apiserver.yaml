{{- if and .Values.rbac.create .Values.apiServer.serviceAccount.create -}}
kind: ServiceAccount
apiVersion: v1
metadata:
  name: {{ .Values.apiServer.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sdc.labels" . | nindent 4 }}
{{- end -}}