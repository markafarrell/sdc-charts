apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-config-store
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sdc.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- toYaml .Values.apiServer.persistentVolume.accessModes | nindent 4 }}
  resources:
    requests:
      storage: "{{ .Values.apiServer.persistentVolume.size }}"
